# Local .terraform directories
.terraform/
.terraform.lock.hcl

# State files (Sensitive Data ပါသဖြင့် Git ပေါ် မတင်ရပါ)
*.tfstate
*.tfstate.backup

# Variable values (Password သို့မဟုတ် Secret များ ပါနိုင်သဖြင့်)
*.tfvars
*.tfvars.json

# Crash log files
crash.log
crash.*.log

# Terraform Local Cache & State Files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Sensitive variable files (Password / Access Keys ပါတတ်သော file များ)
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# IDE / Editor Files
.vscode/
.idea/

terraform.tfvars