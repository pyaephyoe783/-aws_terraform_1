# Local .terraform directories
.terraform/
.terraform.lock.hcl

# State files (Sensitive Data ပါသဖြင့် Git ပေါ် မတင်ရပါ)
*.tfstate
*.tfstate.backup

# Variable values (Password သို့မဟုတ် Secret များ ပါနိုင်သဖြင့်)
*.tfvars
*.tfvars.json

# Crash log files
crash.log
crash.*.log